#include <stdio.h>

int dfs_pair[1000][1000];
int visit[1000];

void DFS(int n, int k)
{
}

int main(void)
{
    int N, M, V;

    scanf("%d", &N);
    scanf("%d", &M);
    scanf("%d", &V);
    for (int i = 0; i < M; i++)
    {
        scanf("%d %d", &x, &y);
        }
}